#!/usr/bin/env bash
set -e
echo ">>> Waiting for local postgres server start..."
wait_db $CLUSTER_NODE_NETWORK_NAME $NODE_PORT $REPLICATION_USER $REPLICATION_PASSWORD $REPLICATION_DB $REPMGR_WAIT_POSTGRES_START_TIMEOUT
sleep "$WAIT_SYSTEM_IS_STARTING"
